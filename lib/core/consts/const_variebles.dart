import 'package:flutter/material.dart';

class ConstVariebles {
  static const edgeInsets = EdgeInsets.symmetric(horizontal: 10, vertical: 10);
  static const iconborder = EdgeInsets.all(8.0);
  static const virticalpadding5 = EdgeInsets.symmetric(vertical: 5);
  static const virticalpadding20 = EdgeInsets.symmetric(vertical: 20);
  static const fullpadding10 = EdgeInsets.all(16);
  static const List<String> choicechipsassignmentList = [
    'All',
    'Upcoming',
    'Missing',
    'Completed',
  ];
}
